---
# Optional extra vars. Prefer vars/accounts.yml for account lists (see accounts.example.yml).

# Legacy single-account (still supported if support_case_accounts is not set):
# support_case_account_name: Parasol
# support_case_account_ids: ['12345678']

# Case / product filters (override group_vars/all/vars.yml defaults):
# activity_date: "2024-01-01"  # defaults to 6 months before run date
